{
  "name": "esnaadm",
  "version": "2.0.0",
  "private": true,
  "description": "EsnaadM is a versatile mobile application developed by jac.mil.ae",
  "main": "dist/main.cjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsc && node rename.cjs && electron .",
    "build": "tsc && node rename.cjs && npx @electron/packager . --platform=win32 --arch=x64 --icon=dist/icons/win/icon.ico --out=./build"
  },
  "author": "a.arif.r@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "@electron/packager": "18.3.6",
    "electron": "35.0.0",
    "fs-extra": "11.3.0"
  }
}
